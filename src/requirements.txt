# Зависимости Umbra с зафиксированными версиями (проверены и рабочие).
# Установка: python -m pip install -r requirements.txt
#
# ВАЖНО (безопасность): python-docx ПИНИМ жёстко. «Второй рубеж» от утечки ПДн
# (md_serializer._ParaRenderer.render и paragraph.text) опирается на то, что в
# 1.2.0 paragraph.text = xpath("w:r | w:hyperlink"). На другой версии, где .text
# считает иначе (напр. без hyperlink), поведение анонимайзера может разойтись с
# сериализатором — не обновлять без перепроверки этого инварианта.
# Сверяйтесь с ФАКТИЧЕСКОЙ версией в окружении: на 1.1.0 нет
# Paragraph.iter_inner_content, и _apply_span_replacements молча уходит на
# запасной путь paragraph.runs. Ставьте ровно 1.2.0.

customtkinter==6.0.0
python-docx==1.2.0
navec==0.10.0
slovnet==0.6.0
razdel==0.5.0
natasha==1.6.0
# pywin32 — только ради win32clipboard (clipboard_util.py). COM-детект открытых
# в Word документов удалён, pythoncom/win32com больше не используются.
pywin32==312 ; sys_platform == 'win32'
pillow==12.3.0
markdown-it-py==4.2.0
mdurl==0.1.2
pypdf==6.14.2
# pdfplumber (MIT) + pypdf (BSD-3) — извлечение текста и таблиц из PDF, на них
# строится карта замен (pdf_reader.py). PyMuPDF (ниже) делает физическую закраску.
# ВНИМАНИЕ ПРО ЛИЦЕНЗИЮ: прежний комментарий здесь запрещал PyMuPDF ради
# проприетарной поставки — это УСТАРЕЛО. Проект перелицензирован на AGPL v3
# именно потому, что PyMuPDF под AGPL (см. README, раздел «Лицензия»).
# Транзитивные зависимости пиним следом: pdfminer.six — MIT, pypdfium2 — BSD-3.
pdfplumber==0.11.7
pdfminer.six==20250506
pypdfium2==5.12.0
openpyxl==3.1.5
defusedxml==0.7.1
pyinstaller==6.21.0
PyMuPDF==1.24.7
tkinterdnd2==0.6.2